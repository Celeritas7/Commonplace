# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python build artifacts (if any subject pulls in Python tooling)
__pycache__/
*.pyc
.venv/
venv/

# Large source files that don't belong in the hub
*.pdf
*.docx
*.xlsx

# Build helper artifacts
build_sandbox.py

# Retired / parked — kept on disk, must not be re-swept by auto-sync's `git add -A`
Coding/Python/fundamentals/python-practice-plus.js
