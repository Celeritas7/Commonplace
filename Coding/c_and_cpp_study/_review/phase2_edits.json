[["C_Book_Lesson_1_2.html", "It alone gives you syntax highlighting, IntelliSense, code navigation, and the debugger.", "VS Code already colours <code>.c</code> files on its own; this extension adds IntelliSense (autocomplete), red error squiggles, code navigation, and the debugger."], ["C_Book_Lesson_1_2.html", "You install VS Code, open a <code>.c</code> file, and notice the syntax highlighting isn't working — everything is plain white text. What's the most likely fix?", "You install VS Code, open a <code>.c</code> file, and notice there is no autocomplete and no red squiggles under mistakes — the code is coloured, but the editor doesn't seem to <em>understand</em> C. What's the most likely fix?"], ["C_Book_Lesson_1_2.html", "You haven't installed the <strong>C/C++ extension</strong> yet. VS Code knows about <code>.c</code> files but doesn't have language support until you add the extension.", "You haven't installed the <strong>C/C++ extension</strong> yet. VS Code can colour <code>.c</code> files by itself, but IntelliSense and error squiggles only arrive with the extension."], ["C_Book_Lesson_1_2.html", "VS Code only highlights JavaScript by default. Switch to a different editor.", "VS Code only understands JavaScript. Switch to a different editor."], ["C_Book_Lesson_1_3.html", "you might end up with <code>\"</code> and <code>\"</code> (curly) instead of <code>\"</code> and <code>\"</code> (straight).", "you might end up with <code>&ldquo;</code> and <code>&rdquo;</code> (curly) instead of <code>\"</code> and <code>\"</code> (straight)."], ["C_Book_Lesson_1_5.html", "<small>You used a name the compiler hasn't seen — typo, missing <code>#include</code>, or you forgot to define a variable / function. Stage: compiler.</small>", "<small>You used a name the compiler hasn't seen — a typo or a missing <code>#include</code>. For a variable GCC says <em>undeclared</em>; for a function it says <em>implicit declaration of function 'foo'</em> and usually adds <em>did you mean …?</em>. Stage: compiler.</small>"], ["C_Book_Lesson_1_5.html", "        <pre>hello.c:4:33: error: expected ';' before 'return'\nhello.c:5:5: error: expected expression\nhello.c:5:12: error: 'return' undeclared\nhello.c:6:1: error: expected ';' before '}' token\nhello.c:6:1: error: expected statement before '}'\nhello.c: At top level:\nhello.c:7:1: error: expected identifier or '('</pre>", "        <pre>hello.c:6:12: error: expected declaration specifiers or '...' before string constant\nhello.c:6:20: error: unknown type name 'x'\nhello.c:7:9: error: initializer element is not constant\nhello.c:8:12: error: expected declaration specifiers or '...' before string constant\nhello.c:8:20: error: expected declaration specifiers or '...' before 'x'\nhello.c:9:5: error: expected identifier or '(' before 'return'\nhello.c:10:1: error: expected identifier or '(' before '}' token</pre>"], ["C_Book_Lesson_1_5.html", "        <pre>hello.c:4 — missing semicolon\n            after printf(...).\n\nAfter fixing that ONE thing,\nevery other error disappears.\n\nThat's the cascade: 6 errors\nshown, only 1 was real.</pre>", "        <pre>hello.c:5 — a stray extra }\n            right after int x = 3;\n            closed main() early.\n\nDelete that ONE character and\nevery other error disappears.\n\nThat's the cascade: 7 errors\nshown, only 1 was real.</pre>"], ["C_Book_Lesson_1_5.html", "<pre>hello.c:5:5: error: 'prinft' undeclared (first use in this function)\n    5 |     prinft(\"Hello\\n\");\n      |     ^~~~~~\n      |     printf</pre>", "<pre>hello.c:5:5: error: implicit declaration of function 'prinft'; did you mean 'printf'?\n    5 |     prinft(\"Hello\\n\");\n      |     ^~~~~~\n      |     printf</pre>"], ["C_Book_Lesson_1_5.html", "Take a slightly bigger program (say, one with 3 or 4 lines inside main). Delete the semicolon after the first line. Compile. Count how many error lines GCC produces. Now fix only the topmost one and recompile. Note how the count drops dramatically.", "Take a slightly bigger program (say, one with 3 or 4 lines inside main). Add an extra closing brace <code>}</code> right after the first line — so main \"ends\" early. Compile. Count how many errors GCC produces. Now delete only that one brace and recompile. Note how the count drops to zero."], ["C_Book_Lesson_1_5.html", "Compile <code>hello.c</code> with <code>gcc -Wall -Wextra -Wpedantic hello.c -o hello</code>.", "Compile <code>hello.c</code> with <code>gcc -O1 -Wall -Wextra -Wpedantic hello.c -o hello</code> (the <code>-O1</code> matters: GCC only notices uninitialized reads while optimizing)."], ["C_Book_Lesson_2_3.html", "<li><code>0b101010</code> — binary (C99 and later, starts with <code>0b</code>). Direct bit specification.</li>", "<li><code>0b101010</code> — binary (starts with <code>0b</code>). Not part of C99 — it is a GCC/Clang extension that only became standard in C23, so don't rely on it in portable code. Direct bit specification.</li>"], ["C_Book_Lesson_2_3.html", "binary (<code>0b101010</code>) — all equal 42. Beware leading-zero octal!", "binary (<code>0b101010</code>, a GCC/Clang extension until C23) — all equal 42. Beware leading-zero octal!"], ["C_Book_Lesson_2_4.html", "Many compilers warn about it (<code>-Wall</code> on by default), but you should still develop the habit of double-checking.", "If you compile with <code>-Wall</code>, GCC warns about it (\"suggest parentheses around assignment used as truth value\") — but <code>-Wall</code> is <em>not</em> on by default, so develop the habit of double-checking."], ["C_Book_Lesson_3_2.html", "<small><code>case 1: int y = 5;</code> can fail to compile because the variable's scope crosses other case labels. Wrap the case body in its own <code>{ }</code> block when you need a local.</small>", "<small><code>case 1: int y = 5;</code> fails to compile in C99: a <code>case</code> label must be followed by a <em>statement</em>, and a declaration isn't one. Wrap the case body in its own <code>{ }</code> block when you need a local.</small>"], ["C_Book_Lesson_3_3.html", "<pre class=\"code\"><span class=\"kw\">int</span> n;\nprintf(<span class=\"str\">\"Enter numbers (0 to stop):\\n\"</span>);", "<pre class=\"code\"><span class=\"kw\">int</span> n, sum = 0;\nprintf(<span class=\"str\">\"Enter numbers (0 to stop):\\n\"</span>);"], ["C_Book_Lesson_3_7.html", "f = fopen(...);<br>if (!f) goto cleanup;<br>buf = malloc(...);<br>if (!buf) goto cleanup;<br>... use them ...<br><span style=\"color:#f9a8d4\">cleanup:</span><br>&nbsp;&nbsp;free(buf); fclose(f);</p>", "FILE *f = NULL; char *buf = NULL;<br>f = fopen(...);<br>if (!f) goto cleanup;<br>buf = malloc(...);<br>if (!buf) goto cleanup;<br>... use them ...<br><span style=\"color:#f9a8d4\">cleanup:</span><br>&nbsp;&nbsp;free(buf);&nbsp;&nbsp;&nbsp;&nbsp;/* free(NULL) is safe */<br>&nbsp;&nbsp;if (f) fclose(f);</p>"], ["C_Book_Lesson_4_5.html", "<h4>🚨 Expecting <code>static int n = compute();</code> to re-run</h4>", "<h4>🚨 Expecting <code>static int n = 0;</code> to re-run</h4>"], ["C_Book_Lesson_4_5.html", "If you wrote <code>static int n = expensive();</code>, <code>expensive()</code> is called just once. That's a feature — but surprising if you expected a reset each call.", "Note that in C the initialiser must be a <em>constant</em> — <code>static int n = expensive();</code> is a compile error (\"initializer element is not constant\"). If you need one-time setup, keep a <code>static int done = 0;</code> flag and check it. Either way the value survives between calls — a feature, but surprising if you expected a reset each call."], ["C_Book_Lesson_5_1.html", "A common fix is a named size: <code>#define N 5</code> (or <code>const int N = 5;</code>), then use <code>N</code> everywhere.", "A common fix is a named size: <code>#define N 5</code> (or <code>enum { N = 5 };</code>), then use <code>N</code> everywhere. (In C a <code>const int</code> is <em>not</em> a compile-time constant, so it can't size an array you initialise with braces.)"], ["C_Book_Lesson_6_3.html", "Compare this with <code>*p = &amp;y</code>'s cousin from 6.2: <code>p = ...</code> moves the arrow, while <code>*p = ...</code> reaches through the arrow to change a value.", "Compare this with <code>*p = 99</code> from 6.2: <code>p = &amp;y</code> moves the arrow, while <code>*p = ...</code> reaches through the arrow to change the value at its tip."], ["C_Book_Lesson_6_6.html", "<small>Precedence trap: <code>*n++</code> increments the <em>pointer</em> then dereferences. To increment the <em>value</em> it points to, use <code>(*n)++</code>.</small>", "<small>Precedence trap: <code>*n++</code> means <code>*(n++)</code> — it reads the value <code>n</code> points to and <em>then</em> moves the pointer to the next element; the value itself is never incremented. To increment the <em>value</em> it points to, use <code>(*n)++</code>.</small>"], ["C_Book_Lesson_6_9.html", "*out = malloc(n * <span class=\"kw\">sizeof</span> <span class=\"kw\">int</span>);", "*out = malloc(n * <span class=\"kw\">sizeof</span>(<span class=\"kw\">int</span>));"], ["C_Book_Lesson_6_9.html", "    \"Keep the (*op) parens, or you've declared something else! 📐\",", "    \"To change the caller's pointer, take int ** and pass &amp;ptr. 🎯\","], ["C_Book_Lesson_7_6.html", "<tr><td class=\"mono plain\">uninitialized read</td><td class=\"mono plain\">reading memory before setting it</td><td class=\"mono plain\">both (Valgrind esp.)</td></tr>", "<tr><td class=\"mono plain\">uninitialized read</td><td class=\"mono plain\">reading memory before setting it</td><td class=\"mono plain\">Valgrind only (ASan doesn't track this)</td></tr>"], ["C_Book_Lesson_7_6.html", "<li><strong>They catch</strong> leaks, use-after-free, buffer overflows, uninitialized reads, and double frees — and tell you where.</li>", "<li><strong>They catch</strong> leaks, use-after-free, buffer overflows, and double frees — and tell you where. Uninitialized reads are Valgrind's speciality; ASan doesn't see them.</li>"], ["C_Book_Lesson_8_1.html", "Run: <code style=\"color:#fff\">Student t = s; t.age = 21;</code>", "Run: <code style=\"color:#fff\">struct Student t = s; t.age = 21;</code>"], ["C_Book_Lesson_8_1.html", "<code>Student t = s;</code> makes <strong>t</strong> a full copy of <strong>s</strong>.", "<code>struct Student t = s;</code> makes <strong>t</strong> a full copy of <strong>s</strong>."], ["C_Book_Lesson_8_1.html", "the arrow operator, coming up next.", "the arrow operator, coming up in 8.4."], ["C_Book_Lesson_8_1.html", "    \"No == on structs; compare member by member. 🟰\",", "    \"One question: what is a struct, really? Think 'bundle of named boxes'. 🎯\","], ["C_Book_Lesson_9_3.html", "This is exactly how editors and databases avoid corrupting your files on a crash.</p>", "This is exactly how editors and databases avoid corrupting your files on a crash. One portability catch: on Windows, C's <code>rename</code> refuses to overwrite an existing file — you have to <code>remove(\"out.txt\")</code> first (losing the atomicity) or use the Windows-specific <code>MoveFileEx</code>. And check the return value: <code>if (rename(...) != 0) perror(\"rename\");</code>.</p>"], ["C_Book_Lesson_10_2.html", "modern C prefers real language features where possible — a <code>const int</code> or an <code>enum</code> for constants,", "modern C prefers real language features where possible — an <code>enum</code> for integer constants (a <code>const int</code> works for ordinary values, but in C it can't size a global array or label a <code>case</code>),"], ["C_Book_Lesson_10_2.html", "Handy for a named constant — though a <code>const int</code> or <code>enum</code> gives you a real type too.", "Handy for a named constant — though an <code>enum</code> gives you a real type too (a <code>const int</code> isn't a true constant in C)."], ["C_Book_Lesson_10_6.html", "    fread(&amp;b-&gt;count, <span class=\"kw\">sizeof</span> b-&gt;count, 1, f);\n    fread(b-&gt;items, <span class=\"kw\">sizeof</span> b-&gt;items[0], b-&gt;count, f);\n    fclose(f);\n}</pre>", "    <span class=\"kw\">if</span> (fread(&amp;b-&gt;count, <span class=\"kw\">sizeof</span> b-&gt;count, 1, f) != 1 ||\n        b-&gt;count &lt; 0 || b-&gt;count &gt; MAX_CONTACTS) {   <span class=\"cm\">// corrupt or truncated file</span>\n        b-&gt;count = 0;\n        fclose(f);\n        <span class=\"kw\">return</span>;\n    }\n    <span class=\"kw\">if</span> (fread(b-&gt;items, <span class=\"kw\">sizeof</span> b-&gt;items[0], b-&gt;count, f) != (size_t)b-&gt;count)\n        b-&gt;count = 0;                          <span class=\"cm\">// never trust a count the file gave you</span>\n    fclose(f);\n}</pre>"], ["Cpp_Book_Lesson_1_2.html", "You skip setting <code>-std=c++20</code> and try to compile this line: <code>auto [a, b] = std::make_pair(1, 2);</code>. What happens?", "You skip setting <code>-std=c++20</code> and try to compile this line: <code>bool ok = name.starts_with(\"Dr.\");</code> (a <code>std::string</code> method added in C++20). What happens?"], ["Cpp_Book_Lesson_1_2.html", "The compiler errors with something like <em>\"structured bindings are a C++17 extension\"</em> — the syntax is rejected because the default standard is older than C++17.", "The compiler errors with something like <em>\"'std::string' has no member named 'starts_with'\"</em> — the method doesn't exist because the default standard (usually C++17 on today's compilers) is older than C++20."], ["Cpp_Book_Lesson_1_2.html", "Most compilers still default to C++14 or C++17. Modern syntax = explicit <code>-std=c++20</code>.' : '✓ <strong style=\"color: var(--green)\">Got it.</strong> The default standard is older than C++17, so newer syntax errors.';", "Most compilers still default to C++14 or C++17. C++20 features = explicit <code>-std=c++20</code>.' : '✓ <strong style=\"color: var(--green)\">Got it.</strong> The default standard is older than C++20, so C++20-only features are rejected.';"], ["Cpp_Book_Lesson_1_4.html", "why linker errors with templates look so scary (the error message includes the full template instantiation chain).", "why <em>compiler</em> errors involving templates look so scary (the message includes the full chain of template instantiations that led to the problem)."], ["Cpp_Book_Lesson_1_5.html", "C++ cascades hard. One missing semicolon or one missing <code>;</code> after a class body can produce <strong>dozens</strong> of follow-on errors.", "C++ cascades hard. One stray brace, or one missing <code>;</code> after a class body, can produce <strong>dozens</strong> of follow-on errors."], ["Cpp_Book_Lesson_1_5.html", "        <pre>hello.cpp:4:5: error: 'cout' was not declared\nhello.cpp:5:5: error: expected ';' before 'return'\nhello.cpp:5:12: error: 'return' undeclared\nhello.cpp:6:1: error: expected ';' before '}'\nhello.cpp:6:1: error: expected statement\nhello.cpp: At global scope:\nhello.cpp:7:1: error: expected unqualified-id\n\n7 errors generated.</pre>", "        <pre>hello.cpp:6:10: error: 'cout' in namespace 'std' does not name a type\nhello.cpp:7:5: error: 'x' does not name a type\nhello.cpp:8:10: error: 'cout' in namespace 'std' does not name a type\nhello.cpp:9:5: error: expected unqualified-id before 'return'\nhello.cpp:10:1: error: expected declaration before '}' token</pre>"], ["Cpp_Book_Lesson_1_5.html", "        <pre>hello.cpp:4 — missing 'std::'\n            in front of cout.\n\nAfter fixing that ONE thing,\nrecompile and ALL 7 errors\ndisappear.\n\nCascade collapses to nothing.</pre>", "        <pre>hello.cpp:5 — a stray extra }\n            right after int x = 3;\n            closed main() early.\n\nDelete that ONE character,\nrecompile, and ALL 5 errors\ndisappear.\n\nCascade collapses to nothing.</pre>"], ["Cpp_Book_Lesson_2_3.html", "The <code>-1</code> assumption silently breaks because comparing an unsigned to signed -1 gets converted.", "Writing <code>== -1</code> happens to work by accident (the <code>-1</code> converts to the same huge unsigned value), but it hides what's really going on — write <code>npos</code> so the intent is clear."], ["Cpp_Book_Lesson_2_3.html", "<code>\"Hello\".substr(1, 3)</code> → <code>\"ell\"</code>.", "<code>std::string(\"Hello\").substr(1, 3)</code> → <code>\"ell\"</code> (a bare <code>\"Hello\"</code> literal has no methods — it has to be a <code>std::string</code> first)."], ["Cpp_Book_Lesson_2_4.html", "<code>cin</code> goes into a <em>failure state</em>. The variable stays at whatever value it had before.", "<code>cin</code> goes into a <em>failure state</em> and (since C++11) the variable is set to <code>0</code> — not the value you wanted, and easy to mistake for real input."], ["Cpp_Book_Lesson_2_6.html", "<code>reinterpret_cast&lt;int&gt;(3.14)</code> does not give you <code>3</code>; it hands you the integer made of the float's raw bit pattern.", "<code>reinterpret_cast&lt;int&gt;(3.14)</code> doesn't even compile — it can't convert between plain number types at all. It works on pointers: <code>reinterpret_cast&lt;int*&gt;(&amp;d)</code> lets you read a <code>double</code>'s raw bytes as if they were an <code>int</code>."], ["Cpp_Book_Lesson_2_6.html", "<code>reinterpret_cast</code> would give you garbage — the raw bit pattern read as an int.</small>", "<code>reinterpret_cast</code> won't even compile for that — it only reinterprets pointers and references, and then you get raw bits, not a converted value.</small>"], ["Cpp_Book_Lesson_3_4.html", "    { text: 'Sum a <code>vector&lt;int&gt;</code>; you only read each value (small, cheap).', answer: 'constref', why: 'Read-only → <code>const auto&amp;</code> is the safe default. (Plain <code>auto</code> copy is also fine for tiny ints.)' },", "    { text: 'Sum a <code>vector&lt;int&gt;</code>; you only read each value (small, cheap).', answer: ['constref', 'auto'], why: 'Read-only → <code>const auto&amp;</code> is the safe default, and for tiny values like <code>int</code> a plain <code>auto</code> copy is just as good — both are right here.' },"], ["Cpp_Book_Lesson_3_4.html", "    if (b.dataset.c === s.answer) {", "    if ([].concat(s.answer).includes(b.dataset.c)) {"], ["Cpp_Book_Lesson_4_6.html", "code: 'std::function<int(int)> f = [](int n){ return n*2; };\\nf = [](int n){ return n + 100; };\\nint main() { std::cout << f(5); }',", "code: 'std::function<int(int)> f = [](int n){ return n*2; };\\nint main() {\\n    f = [](int n){ return n + 100; };\\n    std::cout << f(5);\\n}',"], ["Cpp_Book_Lesson_5_1.html", "std::vector<int> v;\\nv.push_back(10);\\nv.push_back(20);\\nint main() { std::cout << v[1]; }',", "std::vector<int> v;\\nint main() {\\n    v.push_back(10);\\n    v.push_back(20);\\n    std::cout << v[1];\\n}',"], ["Cpp_Book_Lesson_7_3.html", "struct A { virtual int id() { return 0; } };\\nstruct B : A { int id() override { return 1; } };\\nint main() {\\n  A* p = new B();", "struct A { virtual int id() { return 0; } virtual ~A() = default; };\\nstruct B : A { int id() override { return 1; } };\\nint main() {\\n  A* p = new B();"], ["Cpp_Book_Lesson_7_4.html", "code: 'struct Shape { virtual double area() = 0; };\\nstruct Sq : Shape { double area() override { return 4; } };\\nint main() {\\n  Shape* s = new Sq();", "code: 'struct Shape { virtual double area() = 0; virtual ~Shape() = default; };\\nstruct Sq : Shape { double area() override { return 4; } };\\nint main() {\\n  Shape* s = new Sq();"], ["Cpp_Book_Lesson_7_4.html", "Write <code>void report(const Shape&amp; s)</code> that prints <code>s.area()</code>, and call it with both a circle and a rectangle through the base reference.", "Write <code>void report(const Shape&amp; s)</code> that prints <code>s.area()</code>, and call it with both a circle and a rectangle through the base reference. (For that to compile, <code>area()</code> must promise not to change the shape: declare it <code>virtual double area() const = 0;</code> and the overrides <code>double area() const override</code>.)"], ["Cpp_Book_Lesson_7_4.html", "    \"Hold concrete shapes through Shape* / Shape& for polymorphism. 🔁\",", "    \"One question: what does '= 0' do to a class? Think 'contract, not object'. 🎯\","], ["Cpp_Book_Lesson_7_5.html", "They call the destructor for you — but they obey the same rule: if you store a <code>Derived</code> in a <code>unique_ptr&lt;Base&gt;</code> and the base destructor isn't virtual, the derived part still won't be cleaned up. The virtual-destructor rule is fundamental, no matter how the delete is triggered.</p>", "They call the destructor for you — but <code>unique_ptr</code> obeys the same rule: store a <code>Derived</code> in a <code>unique_ptr&lt;Base&gt;</code> with a non-virtual base destructor and the derived part still won't be cleaned up. (<code>shared_ptr</code> happens to remember the real type at construction, so it gets this right even without a virtual destructor — but don't rely on that; make the destructor virtual anyway.)</p>"], ["Cpp_Book_Lesson_7_9.html", "<pre class=\"code\"><span class=\"kw\">struct</span> Animal { <span class=\"kw\">virtual</span> std::string speak() { <span class=\"kw\">return</span> <span class=\"str\">\"...\"</span>; } };\n<span class=\"kw\">struct</span> Dog : Animal { std::string speak() <span class=\"kw\">override</span> { <span class=\"kw\">return</span> <span class=\"str\">\"Woof\"</span>; } };", "<pre class=\"code\"><span class=\"kw\">struct</span> Animal { <span class=\"kw\">virtual</span> std::string speak() <span class=\"kw\">const</span> { <span class=\"kw\">return</span> <span class=\"str\">\"...\"</span>; } };\n<span class=\"kw\">struct</span> Dog : Animal { std::string speak() <span class=\"kw\">const</span> <span class=\"kw\">override</span> { <span class=\"kw\">return</span> <span class=\"str\">\"Woof\"</span>; } };"], ["Cpp_Book_Lesson_7_9.html", "virtual std::string speak() { return \"...\"; }", "virtual std::string speak() const { return \"...\"; }", 4], ["Cpp_Book_Lesson_7_9.html", "std::string speak() override { return \"Woof\"; }", "std::string speak() const override { return \"Woof\"; }", 4], ["Cpp_Book_Lesson_7_9.html", "`<span class=\"kw\">struct</span> Animal { <span class=\"kw\">virtual</span> string speak() { <span class=\"cm\">\"...\"</span> } };<br>` +\n      `<span class=\"kw\">struct</span> Dog : Animal { string speak() override { <span class=\"cm\">\"Woof\"</span> } };<br>` +", "`<span class=\"kw\">struct</span> Animal { <span class=\"kw\">virtual</span> string speak() const { <span class=\"cm\">\"...\"</span> } };<br>` +\n      `<span class=\"kw\">struct</span> Dog : Animal { string speak() const override { <span class=\"cm\">\"Woof\"</span> } };<br>` +"], ["Cpp_Book_Lesson_7_9.html", "Take it by reference instead: <code>void f(const Base&amp; b)</code> (or <code>Base*</code>).</small>", "Take it by reference instead: <code>void f(const Base&amp; b)</code> (or <code>Base*</code>) — and mark the virtual functions you call <code>const</code>, or the <code>const&amp;</code> version won't compile.</small>"], ["Cpp_Book_Lesson_7_9.html", "Change the parameter to <code>const Animal&amp;</code> and confirm it now prints <code>\"Woof\"</code>.", "Change the parameter to <code>const Animal&amp;</code> (declaring <code>speak()</code> as a <code>const</code> member first, as in the lesson) and confirm it now prints <code>\"Woof\"</code>."], ["Cpp_Book_Lesson_8_2.html", "<span class=\"kw\">public</span>:\n    Box(<span class=\"kw\">const</span> Box&amp; other);              <span class=\"hl\">// copy constructor — make a NEW Box from another</span>", "<span class=\"kw\">public</span>:\n    Box();                             <span class=\"cm\">// default constructor (needed for \"Box a;\" below)</span>\n    Box(<span class=\"kw\">const</span> Box&amp; other);              <span class=\"hl\">// copy constructor — make a NEW Box from another</span>"], ["Cpp_Book_Lesson_9_1.html", "<button class=\"fsmode\" data-m=\"str\">max(\"ax\",\"bx\")<small>string</small></button>", "<button class=\"fsmode\" data-m=\"str\">max(string(\"ax\"), string(\"bx\"))<small>string</small></button>"], ["Cpp_Book_Lesson_9_1.html", "<span class=\"cm\">// max(\"ax\", \"bx\") →</span> <span class=\"res\">\"bx\"</span>`,", "<span class=\"cm\">// max(string(\"ax\"), string(\"bx\")) →</span> <span class=\"res\">\"bx\"</span>`,"], ["Cpp_Book_Lesson_9_1.html", "<code>std::string</code> defines <code>&gt;</code> (lexicographic order), so the same body works: <code>\"bx\" &gt; \"ax\"</code> → <span class=\"grn\">\"bx\"</span>.`", "<code>std::string</code> defines <code>&gt;</code> (lexicographic order), so the same body works: <code>\"bx\" &gt; \"ax\"</code> → <span class=\"grn\">\"bx\"</span>. (Bare literals <code>max(\"ax\",\"bx\")</code> would deduce <code>T = const char*</code> and compare <em>addresses</em> — that's why we wrap them in <code>std::string</code>.)`"], ["Cpp_Book_Lesson_9_4.html", "Write a function template <code>template&lt;int N&gt; int sum(const std::array&lt;int, N&gt;&amp;)</code>", "Write a function template <code>template&lt;std::size_t N&gt; int sum(const std::array&lt;int, N&gt;&amp;)</code> (it must be <code>std::size_t</code>, not <code>int</code> — that is the type of <code>std::array</code>'s size parameter, and deduction fails if they don't match)"], ["Cpp_Book_Lesson_10_2.html", "{ <span class=\"cm\">// most specific first</span> }", "{ <span class=\"cm\">/* most specific first */</span> }"], ["Cpp_Book_Lesson_10_2.html", "{ <span class=\"cm\">// any other std exception</span> }", "{ <span class=\"cm\">/* any other std exception */</span> }"], ["Cpp_Book_Lesson_10_2.html", "{ <span class=\"cm\">// catch-all: anything else</span> }", "{ <span class=\"cm\">/* catch-all: anything else */</span> }"], ["Cpp_Book_Lesson_10_3.html", "{ <span class=\"cm\">// handle this one specifically</span> }", "{ <span class=\"cm\">/* handle this one specifically */</span> }"], ["Cpp_Book_Lesson_10_3.html", "{ <span class=\"cm\">// everything else in the tree</span> }", "{ <span class=\"cm\">/* everything else in the tree */</span> }"], ["Cpp_Book_Lesson_10_4.html", "{ <span class=\"cm\">// most specific: handle config problems</span> }", "{ <span class=\"cm\">/* most specific: handle config problems */</span> }"], ["Cpp_Book_Lesson_10_4.html", "{ <span class=\"cm\">// backstop for everything else</span> }", "{ <span class=\"cm\">/* backstop for everything else */</span> }"], ["Cpp_Book_Lesson_10_4.html", "Drop the <code>const</code> or <code>noexcept</code> and you've <em>hidden</em> it, not overridden it. Write <code>const noexcept override</code> so the compiler checks you.", "Drop the <code>const</code> and you've <em>hidden</em> it, not overridden it; drop the <code>noexcept</code> and the compiler rejects the override outright (a looser exception promise isn't allowed). Write <code>const noexcept override</code> so the compiler checks you."], ["Cpp_Book_Lesson_11_2.html", "<code>\"score: \" + 42</code> doesn't compile (and pointer arithmetic if it did would be a disaster).", "<code>\"score: \" + 42</code> silently <em>compiles</em> — it's pointer arithmetic on the literal, pointing 42 characters past it (garbage or a crash when read). <code>std::string s = \"score: \"; s + 42</code> won't compile at all. Either way, not what you meant."], ["Cpp_Book_Lesson_11_2.html", "an unchecked failure leaves it unchanged (or zero-initialised in C++11+).", "since C++11 a failed extraction sets <code>n</code> to <code>0</code> — a value that looks real but isn't."], ["Cpp_Book_Lesson_12_2.html", "    std::string parseString();\n    Array  parseArray();\n    Object parseObject();\n};</pre>", "    std::string parseString();\n    <span class=\"kw\">bool</span>   parseBool();\n    <span class=\"kw\">double</span> parseNumber();\n    Array  parseArray();\n    Object parseObject();\n};</pre>"], ["C_Book_Lesson_3_1.html", "} <span class=\"kw\">else if</span> (score &gt;= 70) {\n    grade = <span class=\"str\">'C'</span>;\n} <span class=\"kw\">else</span> {\n    grade = <span class=\"str\">'F'</span>;\n}</pre>", "} <span class=\"kw\">else if</span> (score &gt;= 70) {\n    grade = <span class=\"str\">'C'</span>;\n} <span class=\"kw\">else if</span> (score &gt;= 60) {\n    grade = <span class=\"str\">'D'</span>;\n} <span class=\"kw\">else</span> {\n    grade = <span class=\"str\">'F'</span>;\n}</pre>"], ["Cpp_Book_Lesson_3_3.html", "This <code>for</code> is exactly equivalent to the <code>while</code> from the last section — same three pieces, just collected together instead of spread out.", "This <code>for</code> has exactly the same shape as the <code>while</code> from the last section — the same three pieces, just collected together instead of spread out (this one counts 0 to 4 rather than 1 to 5)."]]